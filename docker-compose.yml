version: '3.8'

services:
  pdf-converter:
    build: .
    ports:
      - "3000:3000"
      - "5001:5001"
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - SERVER_PORT=5001
